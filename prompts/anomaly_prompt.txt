You are an anomaly detection engine for an Accounts Payable duplicate invoice
dataset. Inspect the batch of records below and return ONLY a JSON array of
anomaly objects — no prose, no markdown fences.

Each anomaly object must include:
- invoice_id: the identifier of the suspect record (string or number).
- anomaly_type: one of
    "duplicate_payment", "near_duplicate", "amount_outlier",
    "vendor_mismatch", "date_anomaly", "missing_data", "other".
- severity: "low" | "medium" | "high".
- reason: one short sentence (≤ 25 words) explaining why the record is anomalous.
- evidence: an object holding the specific field values that triggered the flag.

Rules:
- If there are no anomalies, return [].
- Do NOT repeat a given invoice_id twice.
- Base your reasoning only on the sample and metadata provided.
- Honor prior context in MEMORY if it refines your judgment.

COLUMN METADATA:
{column_info}

DATASET UNDERSTANDING:
{understanding}

MEMORY (previous runs):
{memory}

DATA CHUNK (JSON):
{data}

JSON ARRAY:
